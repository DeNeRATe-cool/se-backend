# oss控制台: https://oss.console.aliyun.com
# oss云存储的EndPoint（地域节点）
alibaba.cloud.oss.endpoint: oss-cn-beijing.aliyuncs.com

spring.application.name: se-backend

server:
  port: 8080

spring:
  servlet:
    multipart:
      enabled: true
  datasource:
    username: root
    password: ilovese666
    type: com.alibaba.druid.pool.DruidDataSource
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://mysql-service.default:3306/course_management
  mail:
    host: smtp.163.com
    username: tluvx0806@163.com
    password: WAgiKzPuYUAbYkFm
    port: 465
    protocol: smtps
    properties:
      mail:
        smtp:
          ssl:
            enable: true
  profiles:
    active: dev
mybatis:
  mapper-locations: classpath:com/se/dao/stuMapper.xml

logging:
  level:
    com.hmdp: debug

jwt:
  secret: "your-256-bit-secret"  # 替换为一个固定的Base64编码密钥
  expiration: 3600               # 过期时间(秒)

