apiVersion: v1
kind: Service
metadata:
  name: se-backend
  namespace: se
spec:
  selector:
    app: se-backend
  type: NodePort
  ports:
    - name: http
      port: 8080
      targetPort: 8080
      nodePort: 30080